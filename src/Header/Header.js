import React from 'react';
import { Navbar, NavItem } from 'react-materialize'

const Header = () => {
    return (
        <Navbar brand="Firebase Chat" right>
            <NavItem href="#">Placeholder</NavItem>
            <NavItem href="#">More Placeholder</NavItem>
        </Navbar>
        )
};

export default Header;